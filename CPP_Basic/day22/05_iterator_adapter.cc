#include <iostream>

using namespace std;


/*
迭代器适配器:
1.back_insert_iterator : 尾部插入的迭代器
2.front_insert_iterator: 头部插入的迭代器
3.insert_iterator: 中间位置插入的迭代器
以上3种都是类型

back_inserter(): 创建一个back_insert_iterator
front_inserter(): 创建一个front_insert_iterator
inserter(): 创建一个 insert_iterator
以上3个是函数, 返回对应的迭代器

*/

int main(int argc, char const *argv[])
{
    
    return 0;
}
